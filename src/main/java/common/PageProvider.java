package common;

import org.openqa.selenium.WebDriver;
import pages.BasePageTest;

public class PageProvider extends BasePageTest {
    public PageProvider(WebDriver driver) {
        super(driver);
    }
}
